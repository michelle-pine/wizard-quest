
function getInitialState() {

  const initialState = {
  };

  return initialState;
}

function rootReducer(state = getInitialState(), action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default rootReducer;
